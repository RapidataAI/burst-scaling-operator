apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "burst-scaling-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "burst-scaling-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "burst-scaling-operator.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "burst-scaling-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
